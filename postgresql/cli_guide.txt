sudo apt install postgresql postgresql-contrib
sudo -i -u postgres OR sudo -u postgres psql ---> LOGIN
psql
\q ---> TO EXIT OUT OF THE POSTGRESQL PROMPT
\l ---> LIST ALL DB

CREATE DATABASE "mbta";

\c ---> CONNECT TO DB

\dt ---> LIST ALL TABLE OF DB

CREATE TABLE "cards" (
    "id" SERIAL,
    PRIMARY KEY("id")
);

\d "NameTable" ---> VIEW SCHEMA OF TABLE


CREATE TABLE "stations" (
    "id" SERIAL,
    "name" VARCHAR(32) NOT NULL,
    "line" VARCHAR(32) NOT NULL,
    PRIMARY KEY("id"),
);

CREATE TYPE "swipe_type" AS ENUM ('enter', 'exit', 'deposit', ...);

CREATE TABLE "swipes" (
    "type" "swipe_type" NOT NULL,
    "datetime" TIMESTAMP NOT NULL DEFAULT now(),
    "amount" NUMERIC(5,2) NOT NULL CHECK("amount" != 0)
);
